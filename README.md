# msh
My SHell

## Notable feature(s)
Commands are space insensitive. The following examples are equivalent
<pre>
- `ls -a -l`
- `ls    -a     -l`
- `              ls            -a            -l`
</pre>

## Build Instructions
To build:
    `make`

To remove build files:
    `make clean`
